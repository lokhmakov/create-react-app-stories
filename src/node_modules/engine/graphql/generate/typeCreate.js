import _                    from 'lodash'

export default function generateTypeCreate(payload) {
  const { name } = payload
  const typeName = _.upperFirst(name)
  return `type ${ typeName }Create {
  result: ${ typeName }
  errors: [Error]
}`
}

import gql                  from 'graphql-tag'

export default function generateQueryDeleted(payload) {
  const { name } = payload
  const query = `subscription ${ name }Deleted {
      ${ name }Deleted
}`

  return gql`${ query }`
}

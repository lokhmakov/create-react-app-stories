import _                    from 'lodash'

export default function generateCustomMutationMap({ mutationMap, name, customResolverMap }) {
  return _.mapKeys(
    _.mapValues(mutationMap, (doc, apiName) => {
      return (parent, payload, ctx) => customResolverMap[apiName](parent, payload, ctx)
    }),
    (doc, apiName) => {
      return `${ name }${ _.upperFirst(apiName) }`
    }
  )
}

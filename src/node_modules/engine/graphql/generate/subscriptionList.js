import { upperFirst }       from 'lodash'

export default function generateSubscriptionList(payload) {
  const { name } = payload
  const typeName = upperFirst(name)

  return [
    `${ name }Created: ${ typeName }`,
    `${ name }Updated: [${ typeName }]`,
    `${ name }Deleted: ID`,
  ]
}

export default ({ config }) => {
  const { name } = config

  return {
    findAll:  `${ name }FindAll`,
    create:   `${ name }Create`,
    created:  `${ name }Created`,
    update:   `${ name }Update`,
    updated:  `${ name }Updated`,
    delete:   `${ name }Delete`,
    deleted:  `${ name }Deleted`,
    subscribeToMore: `${ name }SubscribeToMore`,
  }
}

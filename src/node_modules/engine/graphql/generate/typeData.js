import _                    from 'lodash'

export default function generateTypeUpdate(payload) {
  const { name } = payload
  const typeName = _.upperFirst(name)
  return `type ${ typeName }Data {
  result: [${ typeName }]
  errors: [Error]
}`
}

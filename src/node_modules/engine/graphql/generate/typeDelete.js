import _                    from 'lodash'

export default function generateTypeDelete(payload) {
  const { name } = payload
  const typeName = _.upperFirst(name)
  return `type ${ typeName }Delete {
  result: ID
  errors: [Error]
}`
}

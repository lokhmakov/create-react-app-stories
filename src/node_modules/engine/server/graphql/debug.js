import createDebugger       from 'engine/debug'

import config               from '../../config'

const { name } = require('./package.json')

export default createDebugger(`${ config.name }:${ name }`)

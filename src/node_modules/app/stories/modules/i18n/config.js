import types                 from 'engine/types'

export default {
  name: require(`./package.json`).name,

  fieldList: [
    {
      name: `id`,
      type: types.ID,
      primaryKey: true,
    },

    {
      name: `name`,
      type: types.String,
    },

    {
      name: `lang`,
      type: types.String,
    },

    {
      name: `value`,
      type: types.String,
    },
  ],
}

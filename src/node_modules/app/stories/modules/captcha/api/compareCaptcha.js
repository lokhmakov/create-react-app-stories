import { compare }          from 'bcryptjs'

export default (payload) => {
  try {
    const { captchaValue, captchaText } = payload
    return compare(captchaValue, captchaText)
  } catch (err) {
    throw err
  }
}

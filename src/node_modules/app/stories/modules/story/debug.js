import subsystemConfig      from '../../config'

const name = require('./package.json').name

import createDebugger       from 'engine/debug'
export default createDebugger(`${ subsystemConfig.name }:${ name }`)

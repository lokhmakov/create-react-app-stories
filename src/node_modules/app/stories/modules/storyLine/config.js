import types                 from 'engine/types'

const name = require(`./package.json`).name

export default {
  name,

  fieldList: [
    {
      name: `id`,
      type: types.ID,
      primaryKey: true,
    },
    {
      name: `value`,
      type: types.String,
    },

    {
      name: `story`,
      type: `story`,
    },
  ],
}

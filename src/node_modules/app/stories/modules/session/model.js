import generateModel        from '../../sequelize/generate/model'

import config               from './config'
import models               from './models'

export default generateModel({ ...config, models })

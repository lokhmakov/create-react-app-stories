import { graphql }          from 'react-apollo'

import generateQuery        from './query/signup'

export default ({ config, debug, methods }) => (payload) => {
  return graphql(generateQuery(config), {
    props: ({ mutate }) => ({
      sessionSignup: (variables) => {
        return mutate({
          variables,
        })
      }
    })
  })
}

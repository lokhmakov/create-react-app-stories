import gql                  from 'graphql-tag'

export default function generateQueryCurrent(payload) {
  const query = `query sessionCurrentUser {
  sessionCurrentUser {
    id
    username
  }
}`

  return gql`${ query }`
}

import generateLogin        from './generate/login'

import config               from '../config'
import debug                from '../debug'
import methods              from './methods'

export default generateLogin({ config, debug, methods })

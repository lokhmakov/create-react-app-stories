import generateCurrentUser  from './generate/currentUser'

import config               from '../config'
import debug                from '../debug'
import methods              from './methods'

export default generateCurrentUser({ config, debug, methods })

import types                 from 'engine/types'

export default {
  name: require(`./package.json`).name,

  fieldList: [
    {
      name: `id`,
      type: types.ID,
      primaryKey: true,
    },
    {
      name: `username`,
      type: types.String,
    },
    {
      name: `password`,
      type: types.String,
    },
    {
      name: `accessLevel`,
      type: types.Int,
    },
  ],
}

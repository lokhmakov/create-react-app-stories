import { compare }          from 'bcryptjs'

export default async (payload) => {
  try {
    const { password, passwordHash } = payload
    return compare(password, passwordHash)
  } catch (err) {
    throw err
  }
}
